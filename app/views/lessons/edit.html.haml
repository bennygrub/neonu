.page-header
  = link_to course_lessons_path, class: 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    Back
  = link_to course_lessons_path(@course, @lesson), class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-info-sign
    Show
  %h1 Editing lesson

= render 'form'
